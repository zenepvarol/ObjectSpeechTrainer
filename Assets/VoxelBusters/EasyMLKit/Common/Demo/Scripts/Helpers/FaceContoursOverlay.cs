using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

namespace VoxelBusters.EasyMLKit
{
    public class FaceContoursOverlay : PointsOverlay
    {
        
       /* private List<Face.Contour> m_faceContours;
        
        public void SetFaceContours(List<Face.Contour> contours )
        {
            m_faceContours = contours;
            SetVerticesDirty();
        }


        protected override void AddPoints(VertexHelper vh)
        {
            if (m_faceContours != null)
            {
                foreach (Face.Contour contour in m_faceContours)
                {
                    foreach (Vector2 point in contour.Points)
                    {
                        AddPoint(vh, point);
                    }
                }
            }
        }*/        
    }
}